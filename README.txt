Isto é apenas um text sem sentido para testar essa bagaça

Alteração do README so mesmo pra ver como é que fica.
Interessante mas vamos fazer mais uma Alteração.

git diff
git commit ""
git status
git git add -A
git config
git config --list
git branch